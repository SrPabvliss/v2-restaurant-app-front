export interface ICustomer {
    id: number;
    name: string;
    secondName?: string;
    lastName: string;
    secondLastName?: string;
    email: string;
    phoneNumber: string;
    address: string;
}